import "./tag.scss";
import React, {JSX} from "react";

interface TagProps {
    tagId: number;
    tagName: string;
    userId: string;
}

const Tag = () => {
    return (
        <div>

        </div>
    );
}